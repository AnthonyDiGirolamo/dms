<%= render :partial => 'layouts/flash_messages' %>

<h1>New Share</h1>

<%= render :partial => 'share_form', :object => @share, :as => :share %>

<%# link_to 'Show', document_share_path(@share) %>
<%= link_to 'Cancel', @document, :class => 'icon_cancel action' %>
